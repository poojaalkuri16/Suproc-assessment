{
  "name": "suproc-ai-business-agent",
  "private": true,
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "backend\\.venv\\Scripts\\python.exe -m uvicorn app:app --reload --app-dir backend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}